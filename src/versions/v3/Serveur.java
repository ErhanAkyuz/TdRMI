package versions.v3;

import versions.Player;
import versions.PlayerDispenser;
import versions.PlayerDispenserImp;
import versions.v2.PlayerImp;

import java.net.MalformedURLException;
import java.rmi.Naming;
import java.rmi.RemoteException;
import java.rmi.registry.LocateRegistry;

public class Serveur {


	public static void main(String[] args) throws RemoteException, MalformedURLException, InterruptedException  {
		// mise en place du service de nommage
		LocateRegistry.createRegistry(1099);
		//Instance d'un objet serveur
		PlayerDispenserImp p = new PlayerDispenserImp();
		Naming.rebind("rmi://localhost/pfc", p);
		System.out.println("Le serveur a bien démarré");

		while (true){
			p.waitForNewUrl();
		}

	}
}































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































